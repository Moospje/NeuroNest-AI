# دليل إعادة هيكلة مشروع NeuroNest-AI

هذا الدليل يشرح التغييرات الرئيسية التي تم إجراؤها في مشروع NeuroNest-AI وكيفية الانتقال من البنية القديمة إلى البنية الجديدة.

## التغييرات الرئيسية

1. **إعادة هيكلة المجلدات**: تم إعادة تنظيم المشروع بالكامل باستخدام بنية مجلدات جديدة تتبع أفضل الممارسات.
2. **تحديث التقنيات**: تم تحديث التقنيات المستخدمة لتشمل FastAPI، SQLAlchemy، Next.js، وغيرها.
3. **نظام الوكلاء**: تم تطوير نظام وكلاء ذكي يدعم وكلاء متعددين (Thinker، Developer، AutoGen، CrewAI).
4. **قاعدة البيانات**: تم الانتقال من Firebase إلى PostgreSQL مع دعم الهجرات باستخدام Alembic.
5. **الأمان**: تم تحسين نظام الأمان باستخدام JWT مع دعم تعدد الأجهزة.
6. **التوثيق**: تم إضافة توثيق شامل للمشروع.
7. **الاختبارات**: تم إضافة اختبارات للتأكد من جودة الكود.
8. **النشر**: تم إضافة دعم Docker و Docker Compose لتسهيل النشر.

## البنية الجديدة

```
NeuroNest-AI/
│
├── agents/                   # وكلاء الذكاء الاصطناعي
│   ├── base_agent.py         # الفئة الأساسية للوكلاء
│   ├── thinker_agent.py      # وكيل التفكير التحليلي
│   ├── developer_agent.py    # وكيل البرمجة والمشاكل التقنية
│   ├── autogen_agent.py      # وكيل AutoGen
│   └── crewai_agent.py       # وكيل CrewAI
│
├── api/                      # واجهات REST API
│   └── routes/
│       ├── auth.py           # واجهات المصادقة
│       ├── chat.py           # واجهات المحادثة والرسائل
│       └── agents.py         # واجهات إدارة الوكلاء
│
├── core/                     # مكونات النظام الأساسية
│   ├── orchestrator.py       # منسق الوكلاء
│   ├── memory.py             # إدارة ذاكرة المحادثة
│   └── security.py           # أدوات الأمان
│
├── database/                 # نماذج قاعدة البيانات وأدواتها
│   ├── base.py               # إعداد SQLAlchemy الأساسي
│   ├── models.py             # نماذج قاعدة البيانات
│   └── session.py            # إدارة جلسة قاعدة البيانات
│
├── frontend/                 # تطبيق Next.js للواجهة الأمامية
│
├── config/                   # إدارة الإعدادات
│   └── settings.py           # إعدادات التطبيق
│
├── alembic/                  # أدوات هجرة قاعدة البيانات
│
├── docs/                     # توثيق المشروع
│
├── tests/                    # اختبارات المشروع
│
├── docker-compose.yml        # تكوين Docker Compose
├── Dockerfile                # تكوين Docker للخلفية
└── pyproject.toml            # إدارة الحزم باستخدام Poetry
```

## كيفية الانتقال إلى البنية الجديدة

### 1. نسخ المشروع الجديد

```bash
# نسخ المشروع الجديد
cp -r neuronest-restructured/* /path/to/your/project/
```

### 2. إعداد البيئة

```bash
# تثبيت Poetry
pip install poetry

# تثبيت الاعتماديات
poetry install

# إنشاء ملف .env
cp .env.example .env
# تعديل ملف .env بإعداداتك

# تشغيل هجرات قاعدة البيانات
poetry run alembic upgrade head
```

### 3. تشغيل المشروع

```bash
# تشغيل الخادم الخلفي
poetry run uvicorn main:app --reload --host 0.0.0.0 --port 8000

# تشغيل الواجهة الأمامية
cd frontend
npm install
npm run dev
```

### 4. استخدام Docker

```bash
# بناء وتشغيل الحاويات
docker-compose up -d
```

## ترحيل البيانات

لترحيل البيانات من النظام القديم إلى النظام الجديد، يمكنك استخدام سكريبت الترحيل المتوفر:

```bash
# ترحيل البيانات من MongoDB إلى PostgreSQL
poetry run python scripts/migrate_data.py --mongo-uri mongodb://localhost:27017 --postgres-uri postgresql://postgres:postgres@localhost:5432/neuronest
```

## الميزات الجديدة

### دعم تعدد الأجهزة

يدعم النظام الجديد تسجيل الدخول من أجهزة متعددة:

- كل جهاز يحصل على معرف فريد
- يتم مزامنة المحادثات عبر جميع الأجهزة
- يمكن للمستخدمين إدارة أجهزتهم (عرض، إعادة تسمية، إزالة)

### نظام الوكلاء المتقدم

يوفر النظام الجديد نظام وكلاء متقدم:

- **وكيل التفكير**: متخصص في التفكير التحليلي وحل المشكلات
- **وكيل المطور**: متخصص في البرمجة والمشاكل التقنية
- **وكيل AutoGen**: ينسق بين وكلاء متعددين لحل المشكلات المعقدة
- **وكيل CrewAI**: ينشئ ويدير فريقًا من الوكلاء المتخصصين

### واجهة أمامية حديثة

تم تطوير واجهة أمامية حديثة باستخدام Next.js و Tailwind CSS:

- تصميم متجاوب يعمل على جميع أحجام الشاشات
- دعم الوضع المظلم
- واجهة مستخدم سهلة الاستخدام
- دعم WebSocket للمحادثات في الوقت الحقيقي

## التوثيق

تم توفير توثيق شامل للمشروع:

- **دليل التثبيت**: تعليمات تثبيت المشروع
- **دليل المستخدم**: كيفية استخدام المشروع
- **دليل المطور**: كيفية تطوير المشروع
- **مرجع API**: توثيق واجهات API
- **هندسة المشروع**: شرح هندسة المشروع

## الخطوات التالية

1. **استكشاف الكود**: تعرف على البنية الجديدة واستكشف الكود
2. **تخصيص الوكلاء**: قم بتخصيص الوكلاء وفقًا لاحتياجاتك
3. **تطوير الميزات**: أضف ميزات جديدة باستخدام البنية الجديدة
4. **اختبار**: قم بإجراء اختبارات للتأكد من جودة الكود
5. **نشر**: قم بنشر المشروع باستخدام Docker